import type { VoiceEngine, VoiceInfo } from '..';
type ClonedAPIVoiceInfo = {
    id: string;
    name: string;
    gender?: 'male' | 'female';
    voice_engine?: VoiceEngine;
};
export declare function convertResponseToVoiceInfo(voice: ClonedAPIVoiceInfo): VoiceInfo;
export declare function availableClonedVoices(): Promise<Array<VoiceInfo>>;
export {};
